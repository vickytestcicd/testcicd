name: CI

on: 
  workflow_call:
    inputs:
      environment:
        required: true
        type: string 
    secrets: 
      SONAR: 
        required: true        

  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.environment }}  
   
 
          
