name: CI

on: 
  workflow_call:
    inputs:
      test:
        required: true
        type: string
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.test }}  
   
 
          
