name: CI

on: 
  push:
    branches: [master]
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - id: step1
        run: |
          
      - id: set_var
        run: |
          msg=$(curl -H "Accept: application/vnd.github+json" -k https://api.github.com/repos/vickytestcicd/testcicd)
          content="$msg"
          echo "::set-output name=packageJson::$content"
      - run: |
          echo ${{steps.set_var.outputs.packageJson}}
     #     echo "${{fromJson(steps.set_var.outputs.packageJson).id}}"

#           

#       - id: step1
#         run: | 
#           vicky=($(cat test.txt))
#           echo "::set-output name=test::${vicky[*]}"

#       - run: |
#           a=(${{ steps.step1.outputs.test }})
#           for i in ${a[*]};do echo $i;done
# #       - run: for i in $(cat test.txt);do echo $i;done
          
          
