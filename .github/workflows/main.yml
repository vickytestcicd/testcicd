name: CI

on: 
  push:
    branches: [master]
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo $(ls)
      - run: |
         cd test
         echo $(ls)
      - run: echo $(ls)   
   
 
          
