name: CI

on: 
  workflow_call:
    inputs:
      environment:
        required: true
        type: string 
    secrets: 
      SONAR: 
        required: true        

  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.environment }}  
      - name: check
        id: mongo
        run: exit 0
      - name: test
        if: ${{ steps.mongo.conclusion == 'success' }}
        run: echo 'test'
   
 
          
