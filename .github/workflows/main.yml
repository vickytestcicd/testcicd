name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    uses: vickytestcicd/testcicd2/.github/workflows/main.yml@main
#     runs-on: ubuntu-latest
#     steps:
#       - run: |
#           echo ${{ secrets.ACCOUNT }}
#           echo ${{ secrets.ACCOUNT2 }}
